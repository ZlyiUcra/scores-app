node_modules/
dist/
.env
*.log
.DS_Store
CLAUDE.md

# Local SQLite database (seeded/created at runtime)
*.db
*.db-journal
*.db-wal
*.db-shm

# Legacy local account store (password hashes — never in a public repo)
server/data/users.json

# TypeScript incremental build state
*.tsbuildinfo
